import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  hidden: {
    display: 'hidden',
  },
  formHeader: {
    color: 'purple',
    fontSize: 24,
  },
});

export default styles;
